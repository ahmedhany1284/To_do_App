import 'package:flutter/material.dart';

class New_Task_Screen extends StatelessWidget {
  const New_Task_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'new tasks',

    );
  }
}
